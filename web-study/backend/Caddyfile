# Supply your own DNS name via STUDY_DOMAIN, point its DNS to your host, and
# set PUBLIC_ORIGIN=https://the-same-domain on the Node service.
{$STUDY_DOMAIN} {
  encode zstd gzip
  reverse_proxy 127.0.0.1:8787
  header Strict-Transport-Security "max-age=31536000"
}
